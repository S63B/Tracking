FROM frolvlad/alpine-oraclejdk8:slim
VOLUME /tmp
ADD tracking.jar tracking.jar
RUN sh -c 'touch /tracking.jar'
ENV JAVA_OPTS=""
ENTRYPOINT [ "sh", "-c", "java $JAVA_OPTS -Djava.security.egd=file:/dev/./urandom -jar /tracking.jar" ]
